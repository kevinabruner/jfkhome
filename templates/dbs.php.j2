<?php
// Define the internal svcnames of your database clusters in HAProxy
$db_clusters = [
    ['display' => 'MySQL Cluster {{ env }}', 'svcname' => 'mysql'],
    ['display' => 'PostgreSQL {{ env }}', 'svcname' => 'psql'],];

foreach ($db_clusters as $db):
    $info = get_backend_info($db['svcname'], $stats, "db");
?>
    <div class="service-card db-card">
        <div style="display: flex; align-items: center;">
            <span class="status-dot status-<?php echo $info['status']; ?>"></span>
            <span class="service-name"><?php echo $db['display']; ?></span>
        </div>
        <div class="desc">Database Backend: <code><?php echo $db['svcname']; ?></code></div>
        <div class="tag tag_internal">Infrastructure</div>
    </div>
<?php endforeach; ?>