<?php
// Define the internal pxnames of your database clusters in HAProxy
$db_clusters = [
    ['display' => 'MySQL Cluster', 'pxname' => 'mysql_backend'],
    ['display' => 'PostgreSQL Prod', 'pxname' => 'psql_backend'],];

foreach ($db_clusters as $db):
    $status = get_backend_status_raw($db['pxname'], $stats);
?>
    <div class="service-card db-card">
        <div style="display: flex; align-items: center;">
            <span class="status-dot status-<?php echo $status; ?>"></span>
            <span class="service-name"><?php echo $db['display']; ?></span>
        </div>
        <div class="desc">Database Backend: <code><?php echo $db['pxname']; ?></code></div>
        <div class="tag tag_internal">Infrastructure</div>
    </div>
<?php endforeach; ?>