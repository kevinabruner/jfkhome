- name: Build 
  hosts: repo_jfkhome
  become: true
  tasks:                
    - name: Run first install tasks
      import_tasks: tasks/first_install.yaml

- name: Build 
  hosts: repo_jfkhome
  become: true
  tasks:                
    - name: Generate services web page on jfkhome servers (in reverse proxy repo)
      include_tasks: "{{ git_dir }}/tasks/generate_site.yaml"
